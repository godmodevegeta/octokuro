# Ready-to-use Codex CLI mode: run `codex login`, then copy this file to .env.
# Both http://localhost:3888 and http://<LAN-IP>:3888 work by default.
AI_PROVIDER=codex-cli
HOST=0.0.0.0
PORT=3888
CODEX_CLI_PATH=codex
